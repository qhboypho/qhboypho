# Release preparation — 10 September 2026

Status: isolated staging deployed and tested; **production not deployed**.
No production migrations, payments or shipments were performed. The owner
authorized adding R2 Edit to the existing deploy token, scoped to the QH account.

## Staging rollout evidence and current blocker

- Separate Pages project: https://qhclothes-release-staging.pages.dev/.
- Current staging deployment: `ccb7ca6b`; application bundle from `3a85995b`.
- D1: `qhclothes-release-staging`, `03832596-0ded-4fea-993f-a9a65892d8b7`.
- R2: `qhclothes-release-staging-images`; private chat Worker:
  `qhclothes-live-chat-staging`, version `fe7a1006-d76e-40ea-9338-fa53d36155f9`.
- All 33 migrations applied to real staging D1. Remote D1 rejected nested
  `SELECT CASE ... END` inside the 0031 triggers even though local SQLite and
  Wrangler's local splitter accepted it. Replaced the three checks with
  equivalent `SELECT RAISE(...) WHERE changes() = 0`; actual D1 now accepts them.
  Production still has exactly 0031–0034 pending, so 0031 was not edited after
  production application. The failed staging migration rolled back completely.
- Chat compatibility date corrected from local-calendar September 10 to
  September 9 because Cloudflare rejected a UTC-future date during rollout.
- `npm run check:release` passed again after these fixes, including all 11
  launch suites and zero dependency vulnerabilities. Backup rehearsal passed.
- `scripts/test-staging-release.mjs` checks fixed staging resource IDs and
  absence of provider credentials before making synthetic orders. Verified
  COD creation, idempotent retry, stock decremented once, authenticated admin
  visibility, guest payment-link authorization, missing PayOS config fails
  closed, and exact `SHIPPING_PAYMENT_REQUIRED` rejection before a carrier claim.
  Synthetic orders 1–3 remain for inspection. Temporary admin session was
  removed and the scoped test-phone daily-limit exception was revoked.
- Browser inspected checkout on 390x844 and 1440x900: COD/bank transfer shown,
  MoMo hidden. No real payment or shipment was initiated.
- Maintenance artifact in `ops/maintenance` returned 503/Retry-After/no-store
  in real workerd for 20 method/path cases, then was deployed and verified on
  staging. Staging application was restored and its products API returned 200.
- **Production rollout blocked on legacy deployment access**: the immutable
  `92236a96.qhclothes.pages.dev` URL still executes the old order handler
  (`POST /api/orders` with empty JSON returned 400 Missing required fields,
  creating no order). A maintenance deployment on the canonical domains does
  not replace older hash URLs. Obtain owner approval to restrict deployment
  URLs with Cloudflare Access, verify every legacy/branch alias is protected,
  and leave `qhclothes.pages.dev` / `qhclothes.qzz.io` public. Do not migrate
  until old writers are inaccessible and in-flight requests are drained.
- Production runtime settings contain all three PayOS keys (presence only,
  values not printed); scheduler can read shared DB settings. No pending
  non-cancelled bank-transfer orders were found at this check. Recheck at rollout.
- Independent Luna review could not complete because its quota was exhausted;
  root reviewed and verified the changes. Do not claim an independent approval.

Pages rejects custom `--config` paths. For staging deployment, copy
`wrangler.staging.jsonc` as `.wrangler/staging-site/wrangler.jsonc` and copy
the current `dist` there, then deploy with `--cwd .wrangler/staging-site`,
`--project-name qhclothes-release-staging --branch staging`. Never copy secrets
or production backups. `ops/maintenance-site/wrangler.jsonc` defaults to staging
and deliberately has no database, image or chat bindings.

The remaining production sequence is still Access lock → verify/drain → fresh
private backup/rehearsal → maintenance → migrations + matching Pages/chat →
scheduler → health/reconciliation checks and owner-run payment acceptance.

## Verified production target

- Site: https://qhclothes.pages.dev/ (also has qhclothes.qzz.io).
- Pages project: `qhclothes`; production branch: `ui-new-v1`.
- Observed production deployment: `92236a96-3a64-4434-8faf-c2f604b4da2a`, source `12c4f31`.
- D1: `fashion-store-production`, ID `a1edf97a-0041-438e-84b9-e2ed9e4f6295`.
- Remote migration listing showed only 0031–0034 pending.
- Pages has the three PayOS secret names configured. Values were not printed;
  presence does not prove validity. Scheduler secrets are separate.
- Downloaded Pages configuration has no Durable Object binding. Check again
  at rollout, because another deployment may have changed it.
- Scope includes payment/order hardening commit `4feffda9`, subsequent type and
  deployment fixes, and earlier committed changes since `12c4f31` (including
  product deletion, hidden MoMo and text-UI settings). Review that complete diff
  before authorizing promotion; this is not an isolated cherry-pick of one file.

## Verification evidence

- `npm run typecheck`: strict check passes; no ts-ignore or strict-mode removal.
- `npm run test:launch`: all 11 suites pass, including production bundle build.
- `npm run test:release-runtime`: Pages startup/authentication, real workerd
  chat WebSocket/broadcast, nullable SKU/flash-sale data, mocked marketplace/
  Gemini/Web Push, and shipping-label shared-buffer boundaries pass.
- `npm run test:admin-source-contract`: passes against the current settings UI.
- Full `npm audit`: zero vulnerabilities after build-tool patches and scoped
  `miniflare -> sharp@0.35.4` override. Native sharp resize/decode was checked.
- Both private Workers pass Wrangler deployment dry-runs.
- A clean isolated `npm ci --ignore-scripts` succeeds from the lockfile.
- Production storefront was inspected read-only in a real browser; no order
  was submitted. Earlier desktop/mobile checkout verification used local data.
- Coverage percentage has not been measured; do not claim 80% coverage.

## Private backup rehearsal

Backup resides outside Git under `.wrangler/release-preflight-20260910/`.
Never upload this directory, share its SQL contents, or include it in an artifact.
SHA-256 of the rehearsed export:
`87bd459b3e661ed7d4b51878788b8d80e21a389304350baece67fe7e6eff8b6d`.

The export was loaded into isolated in-memory SQLite and migrations 0031–0034
applied transactionally. Existing order/product/SKU/voucher/user row counts
were preserved; integrity check passed; foreign-key issues were zero; legacy
orders did not acquire new inventory-reservation markers.

Repeat immediately before rollout with a fresh private export:

```powershell
node scripts/rehearse-release-migrations.mjs <private-backup.sql>
```

This proves migration compatibility with the captured data, not an online
staging deployment, production concurrency drain or live provider acceptance.

## Required rollout gates — owner approval first

1. Confirm the full promotion scope and an agreed maintenance window. A write
   pause is required: old checkout writers must not run against new reservation
   triggers. Establish and test the maintenance mechanism before changing D1;
   do not improvise database changes on a busy site.
2. Provision separate staging D1/R2/chat resources before an online preview.
   Preview bindings intentionally inherit none of production's resources.
   Rehearse the candidate there with synthetic credentials/provider mocks.
3. Review the existing namespace inventory and deploy `wrangler.chat.jsonc`.
   The Worker is private (no workers.dev or preview URL), owns the chat class
   and daily expired-chat cleanup. Verify that retention policy is intended.
4. Pause mutations, drain in-flight writes, record the latest deployment, take
   a fresh private backup and rerun the rehearsal. Account for pending payment
   callbacks and ambiguous shipping attempts; never replay them blindly.
5. Apply exactly the four reviewed pending migrations to the verified D1,
   then deploy the matching Pages bundle with `--branch ui-new-v1`. The
   `npm run deploy:prod` command runs the local release gate first but does NOT
   perform the backup, maintenance pause or migrations for the operator.
6. Configure scheduler credentials securely and deploy `wrangler.payments.jsonc`
   only after the matching schema and application are healthy. Do not assume
   Pages secrets are inherited, and do not copy unverified local keys.
7. Verify webhook registration, owner-supervised low-value PayOS acceptance,
   COD order recording, unpaid-order shipping block, and carrier sandbox flow.
   Check scheduler logs and the reconciliation queue before reopening.
8. Monitor order creation errors, duplicate attempts, payment review items and
   carrier timeouts. Do not issue refunds, live shipments or test orders without
   their specific approval.

Do not restore only the old Pages bundle after installing the reservation
triggers. Keep mutations paused and forward-fix, or restore a coordinated
database/application snapshot only after reconciling all intervening external
payments and carrier records. Keep immutable payment audit records intact.

## References

- [Preview deployment access](https://developers.cloudflare.com/pages/configuration/preview-deployments/): hash URLs remain accessible; Access can restrict deployment previews without protecting the canonical site.

- [Cloudflare Pages bindings](https://developers.cloudflare.com/pages/functions/bindings/): Durable Objects must live in a separate Worker.
- [Pages Wrangler configuration](https://developers.cloudflare.com/pages/functions/wrangler-configuration/): external Durable Object binding requires script_name.
- [Sharp 0.35.4 release](https://github.com/lovell/sharp/releases/tag/v0.35.4): patched native-image dependency.
